# Examples

This folder contains a tiny score-table example.

Run it from the repository root:

```powershell
python examples/run_demo.py
```

The demo does not train ADAMM or GAMA. It only shows the basic idea: combine prepared ADAMM-style and GAMA-style score columns, then rank holdout traces by suspiciousness.
